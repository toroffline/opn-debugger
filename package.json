{
  "name": "opn-debugger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "preact": "^10.10.6",
    "preact-router": "^4.1.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "@preact/preset-vite": "^2.3.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  }
}
